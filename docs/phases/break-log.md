# Dziennik prób zepsucia

Każda iteracja: jeden nowy kąt ataku, wynik, a przy błędzie test end-to-end, który failował przed poprawką.

| # | Data | Kąt ataku | Wynik |
|---|---|---|---|
| 1 | 2026-09-16 | 300 równoległych zmian przez CLI (move, edit, link, unlink, odczyt) na 5 taskach, podczas gdy runner przesuwa je przez kolumny z krokami i `goto` | Bez błędu: zero błędów API, serwer żywy, każdy `N.yaml` poprawny, lustro zgodne z dyskiem |
| 2 | 2026-09-16 | Nazwy kolumn z `/`, `next`, spacjami na brzegach | **Błąd.** Kolumna `a/b` była akceptowana, a przesunięcie do niej kończyło się `unsupported`, bo slash rozbija ścieżkę API. `next` kolidowało z `goto: next`. Walidacja w `saveBoard` odrzuca teraz takie nazwy. Test: `TestBoardRejectsUnreachableColumnNames`. Tę część zaczął osierocony przebieg tego samego zadania, niewidoczny w kontekście sesji, a przejęty i sprawdzony w obie strony w tej iteracji |
| 3 | 2026-09-16 | Nazwy kolumn ze znakami specjalnymi URL: `%`, `?`, `#`, `+&=`, spacja, polskie znaki, HTML | **Błąd.** CLI sklejało URL z surowych segmentów: `50%` dawało `invalid URL escape`, a `q?x#y` było ucinane do `q`. Segmenty są teraz kodowane przez `url.PathEscape`. HTML w nazwie kolumny był już bezpiecznie escapowany w UI. Test: `TestColumnNamesThatNeedEscaping` |
| 4 | 2026-09-16 | Osierocony `.git/index.lock` i pliki `.tmp-*`, jakie zostawia `kill -9` w trakcie commitu albo atomowego zapisu | **Błąd.** Po osieroconym locku historia nie dostawała już żadnego commita, a log serwera co 2 s pokazywał błąd gita. Serwer po zdobyciu `flock` usuwa teraz lock i pliki `.tmp-*`, a lock starszy niż minuta także w trakcie pracy. Test: `TestCrashLeftoversAreCleanedOnStart` |
| 5 | 2026-09-16 | 15 razy `kill -9` serwera w losowym momencie, w trakcie tworzenia tasków, przesunięć, kroków `shell` i `agent` | **Błąd.** Crash między zapisem `N.md` a `N.yaml` zostawiał treść bez stanu. Nowe ID liczone tylko z `.yaml` wypadało znowu na `N`, writer słusznie odmawiał nadpisania i od tej chwili każde `item new` kończyło się 409, na zawsze. ID liczone teraz z każdego pliku taska. Test: `TestItemCreationAfterCrashBetweenWrites`. Po poprawce ta sama pętla: 45 potwierdzonych utworzeń i 45 nowych tasków, zero złych plików stanu, zero śmieci, panele kroków równe liczbie tasków `running`, agenci w limicie, historia commituje dalej |
| 6 | 2026-09-16 | Ręczna zmiana nazwy albo usunięcie w `board.yaml` kolumny, w której leżą taski | **Błąd.** Takie taski znikały z boardu w API i w przeglądarce. Task, który był już `done`, nie miał ACTION REQUIRED, więc nie pokazywał się nigdzie poza `kanban item N`. Board dokłada teraz na końcu kolumny spoza `board.yaml` z `missing: true`, w przeglądarce z przerywaną ramką i dopiskiem "not in board.yaml". Test: `TestTasksInRemovedColumnStayVisible` |

